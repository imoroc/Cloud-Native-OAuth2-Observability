[project]
name = "oauth2"
version = "0.1.0"
description = "RFC 6749 compliant OAuth2 authorization server"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "locust>=2.43.4",
    "prometheus-client>=0.24.1",
]
